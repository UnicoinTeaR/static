$(function() {
	// 跳push页面
	// $(".hrefIcoPush").on("click",function(){
	// 	var dataHref  = $(this).attr("data-href");
	// 	//获取域名
     //   var locations =  window.location;
     //   var domain = locations.protocol+"://"+locations.hostname;
     //   if(/[A-Za-z]*$/.test(locations.hostname)){
     //       // 以及域名直接加ico
     //       if(locations.hostname.split(".").length<3){
     //           domain = locations.protocol+"//"+"ico."+locations.hostname;
     //       }else{
     //           domain = locations.protocol+"//"+"ico"+locations.hostname.substring(locations.hostname.indexOf("."));
     //       }
	//    }
     //   window.location.href = domain+dataHref;
	// });
});